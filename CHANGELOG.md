# Changelog

## Unreleased

- Plan todo pass: CloudWatch dashboard + explicit Lambda log groups; `POST /v1/kbs/{id}/search` + `vector_stub`; Bedrock guardrails passthrough; worker S3 extract; Dockerfile; `packages/sdk-python` stub; isolation + OpenSearch template tests; CLI `kb-create` / `job-create`; web `JobTimeline`; runbooks + GDPR stub; `generate_commit_ladder.py`; `backfill_history.py --onto-main`; CI SBOM job; OpenAPI paths expanded.

## 0.1.0

- Initial monorepo: CDK infra, control plane Lambda, worker image, contracts, web SPA, CLI, observability docs, git history tooling.
