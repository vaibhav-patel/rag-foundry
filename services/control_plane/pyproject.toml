[project]
name = "rag-foundry-control-plane"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["boto3>=1.35.0"]

[project.optional-dependencies]
dev = ["pytest>=8.0.0", "moto[s3,dynamodb,stepfunctions,bedrock-runtime]>=5.0.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
